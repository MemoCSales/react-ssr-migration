# react-ssr-migration
Migrates the Kotlin website homepage to a React Router 7 Framework Mode application with Server-side rendering, preserving the original UI, styling, and interactive behavior while improving structure and maintainability.
